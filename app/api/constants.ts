export enum Objects {
    Items = 'items',
    Annotations = 'annotations',
    Events = 'events',
    Predictions = 'predictions',
}

export enum Actions {
    Create = 'create',
    Update = 'update',
    List = 'list',
    Detail = 'detail',
    Delete = 'delete',
    SetEventReady = 'set event ready',
    Vote = 'vote',
}

export enum HttpMethods {
    GET = 'GET',
    POST = 'POST',
    PUT = 'PUT',
    DELETE = 'DELETE',
}
